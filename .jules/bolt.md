## 2026-04-06 - [ABD Matrix Assembly and Inversion Optimization]
**Learning:** In `Laminate.update`, assembling the 6x6 ABD matrix using `np.vstack` and `np.hstack` adds significant array allocation overhead compared to pre-allocating an `np.empty((6, 6))` array and filling it via slicing. Furthermore, immediately computing the compliance matrix (`abd`) via `np.linalg.inv(self.ABD)` is unnecessary and computationally expensive for laminates that are discarded early (e.g., failing buckling constraints) and do not require full failure analysis.
**Action:** When assembling small block matrices, allocate an empty array and use slicing for a 3-4x speedup. Implement lazy evaluation (using `@property` and a hidden cache attribute) for expensive matrix inversions so they are only computed on-demand.
## 2026-04-06 - [ABD Matrix Assembly and Inversion Optimization]
**Learning:** In `Laminate.update`, assembling the 6x6 ABD matrix using `np.vstack` and `np.hstack` adds significant array allocation overhead compared to pre-allocating an `np.empty((6, 6))` array and filling it via slicing. Furthermore, immediately computing the compliance matrix (`abd`) via `np.linalg.inv(self.ABD)` is unnecessary and computationally expensive for laminates that are discarded early (e.g., failing buckling constraints) and do not require full failure analysis.
**Action:** When assembling small block matrices, allocate an empty array and use slicing for a 3-4x speedup. Implement lazy evaluation (using `@property` and a hidden cache attribute) for expensive matrix inversions so they are only computed on-demand.

## 2026-04-06 - [ABD Matrix Assembly and Inversion Optimization]
**Learning:** In `Laminate.update`, assembling the 6x6 ABD matrix using `np.vstack` and `np.hstack` adds significant array allocation overhead compared to pre-allocating an `np.empty((6, 6))` array and filling it via slicing. Furthermore, immediately computing the compliance matrix (`abd`) via `np.linalg.inv(self.ABD)` is unnecessary and computationally expensive for laminates that are discarded early (e.g., failing buckling constraints) and do not require full failure analysis.
**Action:** When assembling small block matrices, allocate an empty array and use slicing for a 3-4x speedup. Implement lazy evaluation (using `@property` and a hidden cache attribute) for expensive matrix inversions so they are only computed on-demand.

## 2026-03-09 - [NumPy Overhead & Algebraic Simplifications in Tight Loops]
**Learning:** Functions like `np.linspace` or computing exponents (`zk**3`) using Numpy abstractions introduce unnecessary overhead for smaller datasets in tight loops (e.g. `Laminate.update` calling `_calculate_z_coords`). Using simpler alternatives like `np.arange` with native math operations, and factorizing array arithmetic `(zk**2 - zk_1**2)` to `(zk - zk_1) * (zk + zk_1)` drastically reduces execution time. Also, keeping intermediate mathematical terms (like pre-computing `U2 * cos2` to reuse) directly in the local namespace rather than making array allocations helps minimize repetitive matrix processing times.
**Action:** When working with repetitive inner loops over small numpy arrays, rely on algebraic factorizations over default vector exponentiation rules. Also, avoid higher level numpy features like `np.linspace` and use simpler math against initialized `np.arange` elements instead.

## 2026-03-10 - [Avoid Transpose and Reshape in Tight Vectorized Loops]
**Learning:** In highly vectorized numerical code (like coordinate transformation and stiffness matrix generation in `lamina/clt.py`), using standard NumPy idioms like `np.array([...]).transpose()` or creating a multidimensional array `(3, 3, N)` just to immediately call `.reshape(9, -1)` introduces surprising memory allocation overhead. Native slicing into pre-allocated `np.empty()` arrays (e.g. `res[0] = Q_bar_11` for a `(9, N)` array instead of `(3, 3, N)`) bypasses intermediate object creation and results in ~15-20% speedups. Similarly, relying on `np.divide` directly with a context manager `with np.errstate(divide='ignore'):` is up to 20% faster than boolean array masking (`mask = a != 0; res[mask] = 1 / a[mask]`).
**Action:** When a method generates a multi-dimensional array that the caller immediately reshapes or flattens, refactor the generator to return the flat shape directly via `np.empty()`. Replace boolean masks guarding division with `np.errstate` when the array size justifies skipping the double-pass of mask creation and masked assignment.

## 2026-04-06 - [Vectorized Optimizations using errstate and np.where]
**Learning:** In highly vectorized operations with small arrays (like solving the Tsai-Wu criterion for laminates), explicitly calculating masks and instantiating empty/full subset arrays based on valid ranges causes significant object allocation overhead inside loops. By calculating potential linear and quadratic solutions globally leveraging `np.errstate(divide='ignore', invalid='ignore')`, we completely bypass Python array loops and boolean masking sub-assignments. We also found out replacing broadcast mechanisms like `eps0[:, np.newaxis] + kap[:, np.newaxis] * z[np.newaxis, :]` with simple vector algebra like `ex = eps0[0] + kap[0] * z` avoids costly intermediary 2D/3D array construction. These micro-optimizations combined yielded around 20-30% faster execution.
**Action:** Use `np.errstate` along with `np.where` for branchless logic in vectorized code rather than manually masking valid values, and prefer native Python math / component-based logic over creating large broadcasted axes for small dimension arithmetic.

## 2026-04-06 - [ABD Matrix Assembly Optimization]
**Learning:** In `lamina/clt.py`, the flat-array multiplication for assembling A, B, and D matrices using the `@` operator (`(Q_bars_flat @ h).reshape(3, 3)`) is slightly slower than using `np.dot` directly (`np.dot(Q_bars_flat, h).reshape(3, 3)`). The `@` operator introduces overhead for checking dimensions and broadcasting when dealing with combinations of flat vectors and 2D arrays, whereas `np.dot` maps more directly to lower-level BLAS routines for simple 2D x 1D dot products. For matrices of shape (9, N) x (N,), `np.dot` provides a noticeable micro-optimization.
**Action:** Use `np.dot` instead of the `@` operator when performing matrix-vector multiplication with flat arrays or simple 2D x 1D dot products in hot paths to bypass operator overhead.

## 2026-04-06 - [Avoid np.linspace for Small Arrays in Hot Paths]
**Learning:** Using `np.linspace(0, 2*np.pi, num_points)` in hot paths like failure envelope generation (`FailureCriterion` methods) introduces measurable function abstraction overhead when calculating stresses over a small number of points. Replacing it with `np.arange(num_points, dtype=np.float64) * (2 * np.pi / (num_points - 1))` is approximately 3-4x faster because it bypasses `np.linspace`'s internal validation, broadcasting, and endpoint handling, directly executing simpler native array math.
**Action:** For small, fixed-size numerical array generation in performance-critical loops or functions that run frequently, prefer `np.arange` combined with simple algebraic scaling over higher-level abstractions like `np.linspace`.

## 2026-04-06 - [Algebraic Expansion of 3x3 Compliance Transformations]
**Learning:** In `lamina/clt.py`, calculating stiffness polars by rotating the compliance matrix using full matrix multiplications (`T_epsilon @ a @ T_sigma_inv`) in a loop adds significant overhead because `_get_transformation_matrices` creates and populates two intermediate 3x3xN matrices. Algebraically expanding the transformation (`S' = T_epsilon @ S @ T_sigma_inv`) into direct formulas for `a00`, `a11`, and `a22` using invariants completely avoids allocating and multiplying these temporary arrays. This simple algebraic reduction speeds up the `polar_stiffness` execution by ~33%.
**Action:** When repeatedly transforming small 3x3 stiffness or compliance matrices over many angles (e.g. for polar plots), explicitly write out the algebraic transformation equations using invariants instead of relying on generalized multidimensional array multiplications.

## 2026-04-06 - [Vectorized Optimizations using np.square and nested np.where]
**Learning:** In highly vectorized numerical calculations involving quadratic equations (like Tsai-Wu failure criteria), calculating boolean array masks (`mask_linear = np.abs(A) < 1e-10`) and allocating explicit subsets for linear vs quadratic roots introduces high array sub-assignment overhead in tight loops. A fully branchless approach that computes all possible linear and quadratic roots globally and then uses nested `np.where` functions inside an `np.errstate` context to select valid answers avoids subset array instantiation entirely, yielding a ~1.77x performance boost. Furthermore, replacing `B * B` with `np.square(B)` is the most efficient and readable convention.
**Action:** When selecting between different mathematical branches on large arrays, prefer computing all branches inside `np.errstate(divide='ignore', invalid='ignore')` and using nested `np.where` combinations over creating separate masked arrays and sub-assigning back to the original array.

## 2026-04-06 - [Avoid Multiplying by Zeroed Rows in Matrix Operations]
**Learning:** In `FailureCriterion._get_stresses_vectorized`, creating a full 6xN matrix where rows 2-5 are zeros and performing a full matrix multiplication `laminate.abd @ NM` wastes significant compute on zero operations. Explicitly explicitly allocating only the non-zero rows of the NM matrix (`NM_reduced`, shape 2xN) and multiplying by sliced non-zero columns of `laminate.abd[:, :2] @ NM_reduced` avoids these redundant operations, yielding roughly a ~2x performance speedup without changing the output.
**Action:** When performing matrix multiplication where one matrix contains rows or columns of known zeros, explicitly slice the matrices to exclude the zeroed dimensions.

## 2026-04-06 - [Tsai-Wu Positive Root Mathematical Reduction]
**Learning:** The quadratic term `A` in the Tsai-Wu failure criterion calculation (`A * f^2 + B * f - 1 = 0`) is mathematically strictly non-negative because the material limits form a positive-definite matrix (`F11*F22 > F12^2`). Consequently, the discriminant `delta` is always positive and `sqrt(delta) >= |B|`. This means `f1_quad = (-B + sqrt(delta)) / 2A` is guaranteed to be the only valid, positive root.
**Action:** Avoid allocating and testing arrays for negative quadratic roots (`f2_quad = (-B - sqrt_delta) / 2A`) and discriminant bounds checks (`delta >= 0`) in Tsai-Wu optimizations. Removing this defensive nesting of `np.where` reduces branch overhead significantly without altering mathematical correctness.

## 2026-04-06 - [Hoist Object-Invariant Matrices Out of Vectorized Loops]
**Learning:** For performance-critical loops evaluating physics criteria across many angles (like failure envelopes), calculating properties that depend exclusively on the physical laminate material and layup (e.g., ply stiffness matrices `K_all = Q @ T_all` and transformed midpoints `K_all_z`) inside the evaluation method adds enormous redundant overhead (e.g., repeating a `Q @ T_all` multiplication across hundreds of loop iterations).
**Action:** Hoist these structurally-invariant calculations into the object's initialization or update phase. Caching matrices like `K_all` prevents redundant array allocations and multidimensional matrix multiplications inside hot execution paths, substantially speeding up iterations.

## 2026-04-06 - [Lazy Evaluation of Failure Transformation Matrices]
**Learning:** In `Laminate.update`, computing failure-specific matrices (`K_all` and `K_all_z`) for every instantiated laminate adds significant array allocation overhead (creating `(N, 3, 3)` arrays and performing matrix multiplications). However, algorithms like `GeneticAlgorithm` create and discard thousands of laminates solely for properties or buckling analysis, never needing failure transformation matrices. Making them lazy properties avoids this overhead entirely, speeding up laminate creation by ~34%.
**Action:** Implement lazy evaluation (`@property`) for domain-specific arrays that are not universally required by all consumers of an object to avoid computing them for objects that may be discarded early.

## 2026-04-06 - [Avoid Multiplying by Zeroed Columns in Matrix Operations]
**Learning:** In `calculate_safety_factor`, multiplying the 6x6 `ABD_inv` matrix by a 6-element load vector padded with zeros `[Nx, Ny, Nxy, 0.0, 0.0, 0.0]` wastes floating point operations. Explicitly slicing the matrix to exclude the zeroed dimensions (`ABD_inv[:, :3] @ [Nx, Ny, Nxy]`) eliminates 50% of the math and yields a ~20% performance increase in `GeneticAlgorithm` evaluations.
**Action:** When performing matrix multiplication where one matrix contains rows or columns of known zeros, explicitly slice the matrices to exclude the zeroed dimensions.

## 2026-04-06 - [Geometric Properties (h2, h3) Calculation Optimization]
**Learning:** In `Laminate.update`, calculating the geometric ply properties `h2` and `h3` using algebraic factorization like `h * (zk*zk + zk*zk_1 + zk_1*zk_1)` requires creating and storing multiple intermediate arrays. Since `h2 = zk^2 - zk_1^2` and `h3 = zk^3 - zk_1^3`, mathematically reducing these into differences of squares/cubes and using `np.square()` (e.g., `zk_sq = np.square(zk); h3 = zk_sq * zk - zk_1_sq * zk_1`) minimizes temporary object creation and executes ~30% faster in benchmarks for this specific formula.
**Action:** When computing multi-term algebraic polynomials over arrays (like geometric integration limits `zk`), simplify the math to evaluate direct power differences (using native `np.square()` or simple multiplication) rather than factored polynomial forms to minimize intermediate array allocations.
